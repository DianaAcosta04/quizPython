#lista de palabras
palabras = ["perro", "gato", "elefante", "oso", "jirafa"]

#usamos la funcion lambda para las palabras de >=5
filtrar = list(filter(lambda palabra: len(palabra) > 5, palabras))

#impreme el resultado
print(filtrar)
